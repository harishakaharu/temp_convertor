import { StyleSheet } from "react-native";
export const appStyle = StyleSheet.create({
  main: { flex: 1, justifyContent: "center", padding: 20 },
  backgroundImg: { height: "100%", width: "100%" },
});
